# UU Community is a Next.js app — GitHub Pages static deploy is disabled.
# Host for free on Vercel instead (see README.md).

name: pages-disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Vercel Hobby for UU Community. Static Pages deploy retired."
